import React from "react";
import Base from "../core/Base";

const UserDashBoard = () => {
  return (
    <Base title="Userdashboard page">
      <h1>This the the user dashboard page</h1>
    </Base>
  );
};

export default UserDashBoard;
